<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shaochengyang.deltaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.authentication.login.LoginActivity" />
        <activity android:name=".ui.seatreserve.ecoseat.EcoSeatReserveActivity" />
        <activity android:name=".ui.seatreserve.firstclassseat.FirstClassSeatReserveActivity" />
        <activity android:name=".ui.ChartActivity" />
        <activity android:name=".ui.selectflight.SelectFlightActivity" />
        <activity android:name=".ui.displayflight.FlightDetailActivity" />
        <activity android:name=".ui.flightconfirmation.ConfirmationPageActivity" />
        <activity android:name=".ui.compare.CompareActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".ui.purchaseticket.PurchaseTicketActivity" />
        <activity android:name=".ui.compareshowflight.DemoFlightDetailActivity" />
        <activity android:name=".ui.compareshowflight.DemoConfirmationPageActivity" />
        <activity android:name=".ui.payment.PaypalActivity">

        </activity>
    </application>

</manifest>